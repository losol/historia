---
"@eventuras/historia": patch
---

Form blocks: a required checkbox now shows "This field is required" when left unticked, and an invalid email address gets its own message ("Please enter a valid email address") instead of the required-field one, on optional fields too.
