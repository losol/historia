# Generated by the Aspire CLI (`aspire restore` / `aspire run`).
.aspire/
dist/
# Aspire installs with `pnpm install --ignore-workspace`, which writes its own
# lockfile. The workspace lockfile at the repo root is the one that counts: it
# is resolved under the supply-chain settings in pnpm-workspace.yaml.
pnpm-lock.yaml
